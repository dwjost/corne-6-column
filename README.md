# 6 Column Corne Layout

The firmware may be built by running `make` in this directory.  Podman or Docker is required.

The ZMK docker container will be acquired, which also includes a release of the Zephyr SDK.

## Building Locally on a Linux System

In order to build locally, Zephyr SDK is required to be installed...

*TBD*
